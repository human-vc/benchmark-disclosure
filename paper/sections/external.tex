\section{Evidence from a Published Leaderboard}\label{sec:external}

Everything established so far concerns a percentile constructed here, on a panel assembled here, so the boundary may be a property of that construction rather than of leaderboards in general. That possibility can be checked outside the panel. HELM Lite publishes its full accuracy table at every versioned release, archives the earlier releases, and reports a pool-relative headline, so the same evidence can be read at fourteen points in time while the set of models being compared grows underneath it \citep{liang2022helm}.

The first instinct on hearing that a leaderboard moved is that the benchmark suite was revised, so the invariants come first. Across releases v1.0.0 through v1.13.0 the ten scenario columns are identical, with no scenario added, dropped, or renamed apart from a cosmetic relabelling of the model column header at v1.10.0. The same 24 models appear in all fourteen releases, and their 240 published scenario scores are bit-identical across the entire sequence. Over the same span the evaluated pool grows from thirty models to ninety-one.

Against that frozen evidence, all twenty-four of the constant models see their published Mean win rate change, by 0.1232 on average in absolute value and by as much as 0.2178 for Mixtral 8x7B, which falls from 0.7276 to 0.5098. Most of that movement is arithmetic and should be conceded immediately. Mean win rate is the fraction of head-to-head comparisons a model wins against the others evaluated, so a fixed model faces a larger and later set of competitors as the pool grows, and the change in a model's win rate correlates with its initial standing at $-0.766$, the pattern a rising pool produces.

The load-bearing fact is elsewhere. If the drift were a monotone transform of the frozen evidence it would be harmless, because a leaderboard exists to order models and a monotone rescaling preserves every order. It is not monotone. Of the 24, twenty-two fall and two rise, and five of the 276 pairs among them reverse between the first release and the last. \autoref{fig:helm} draws the reversing models in colour against the rest: the coloured lines cross while the evidence beneath them is fixed, which is the whole of the claim. The published numbers are correct as defined at every release. What changed was the pool, and the published order followed the pool. \citet{singh2025leaderboard} report a comparable pattern from a different mechanism, deprecation policy rather than a coverage boundary changing which models remain available, and in both the reference set rather than the evidence moves the ranking. Mixtral 8x7B and PaLM-2 Bison stand at 0.728 and 0.686 in the first release and at 0.510 and 0.526 in the last.

The control comes from the same organisation. HELM Capabilities runs on the same infrastructure and the same release cadence, and its pool grows comparably, from twenty-two models to sixty-eight over sixteen releases, but its headline is a Mean score, an absolute average over scenarios rather than a fraction of comparisons won. The same 22 models appear in every release, their 110 published scenario scores are bit-identical, and none of the twenty-two headline values move at all. No pair reorders, at the endpoints or ever. That prediction, zero drift under an absolute headline with the same pool growth, was stated before the test, and it is the cleanest evidence that the mechanism is the pool-relative aggregation and not the evaluation practice, the suite, or the organisation. \autoref{fig:helm} places the two leaderboards side by side.

The reading to avoid is that HELM made an error. It did not, and the existence of an absolute headline on HELM's own Capabilities leaderboard is the point rather than an embarrassment. The economic content lies in what a relative certificate can carry. A pool-relative headline is a joint statement about a model and about the population evaluated beside it, so two such numbers need not be comparable even when every measurement behind them is shared, and read later as statements about the models alone they move standing between providers for reasons belonging to neither. Restoring comparability across moments is the classical equating problem \citep{kolen2014}, and measures placing models on an absolute scale estimated jointly across benchmarks avoid the difficulty entirely \citep{epoch2026rosetta}. The panel and the leaderboard are two instances of one boundary: both order a model against whichever peers happen to be present, so the composition of that set carries part of what reads as a statement about the model.

\begin{figure}[t]\centering
\input{figures/fig_helm}
\caption{The same boundary on a published leaderboard, with its control. Left:
the 24 models present in all fourteen HELM Lite releases, whose 240 published
scenario scores are bit-identical throughout, plotted by their published Mean
win rate. Models belonging to the five reordering pairs are highlighted. Right:
HELM Capabilities over sixteen releases, same organisation and comparable pool
growth, whose headline is an absolute mean rather than a pool-relative one.
Nothing moves.}
\label{fig:helm}
\end{figure}
